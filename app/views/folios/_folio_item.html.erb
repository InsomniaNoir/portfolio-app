<div class="col-md-4">
  <div class="card mb-4 box-shadow" data-id="<%= folio_item.id %>">
    <%= image_tag folio_image(folio_item.thumb_image.url, 'main'), class: "card-img-top" %>
    <div class="card-body">
      <p class="card-text">
        <span><%= link_to folio_item.title, folio_show_path(folio_item.id) %></span> <%= folio_item.subtitle %>
      </p>
      <div class="d-flex justify-content-between align-items-center">
        <!-- <div class="btn-group">
          <%= link_to "View", folio_path(folio_item), class: "btn btn-sm btn-outline-secondary" %>
          <%= link_to "Edit", edit_folio_path(folio_item), class: "btn btn-sm btn-outline-secondary" %>
          <button type="button" class="btn btn-sm btn-outline-secondary">View</button>
          <button type="button" class="btn btn-sm btn-outline-secondary">Edit</button>
        </div> -->
        <small class="text-muted">9 mins</small>
      </div>
    </div>
  </div>
</div>
